//style components that we can later use HTML inside javascript files
export const col = {
  border: "#e0e0e0 1px solid"
};

//for the weekdays with name of the day and date
export const weekDays = {
  height: 100
};

//name of the days
export const weekDayName = {
  fontSize: 12,
  lineHeight: "32px",
  textTransform: "capitalize",
  color: "#757575",
  marginLeft: 10
};

//for the calendar container
export const container = {
  paddingRight: "10em",
  paddingTop: "2em",
  paddingBottom: "2em",
  backgroundColor: "white"
};

//for the week dates
export const weekDates = {
  fontSize: 40,
  lineHeight: "12px",
  color: "#9e9e9e",
  marginLeft: 10
};

// for each time slot in whole calendar
export const slot = {
  height: 100,
  cursor: "pointer"
};

//time column on the left
export const timeCol = {
  fontSize: 10,
  color: "black",
  textAlign: "right",
  padding: 6
};

//time string style
export const timeString = {
  position: "absolute",
  right: 8,
  top: -8
};

export const row = {
  position: "relative"
};

// week toolbar
export const toolbar = {
  paddingBottom: 10
};

export const toolbarDate = {
  fontSize: 20,
  fontWeight: 400,
  opacity: 0.54,
  lineHeight: "30px",
  textAlign: "right"
};

//for the appTitle
export const appTitle = {
  fontSize: 20,
  fontWeight: 5000,
  lineHeight: "30px",
  color: "#3175AD"
};

export const alignRight = {
  textAlign: "right"
};

export const spacify = {
  marginRight: 5
};

export const inputStyles = {
  marginTop: 25,
  marginBottom: 15
};

export const colorg = {
  backgroundColor: "red"
};
// for previous and next button
export const weekButtons = {
  paddingRight: 0,
  paddingLeft: 14,
  textAlign: "center"
};

//lighthighlighter for marking today date
export const lightHighlighter = {
  backgroundColor: "rgba(0,0,0,0.04)"
};

//eventhighlighter when we create an event
export const eventHighlighter = {
  position: "absolute",
  backgroundColor: "#3175AD",
  border: "1px solid #81cdf2",
  borderRadius: "4px",
  color: "white",
  padding: "2px 4px",
  fontSize: "12px",
  zIndex: 1,
  cursor: "pointer"
};
